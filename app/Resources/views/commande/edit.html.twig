{% extends 'dashboard.html.twig' %}

{% block fos_user_content %}
   <div>
    <section>
        <ol class="breadcrumb active">
            <li><a href="{{ path('commande_index') }}"> Liste Des Commandes </a></li>
            <li><a href="#"> Les Informations De La Commande </a></li>
            <li><a href="#"> Modifier Les Informations </a></li>
        </ol>
    </section>
    <section class="content">
        <div class="row">
            <div class="col-md-8" style="margin-left: 150px">
                <div class="box box-primary">
                    <div class="box-header with-border">
                        <h2 class="box-title"> <strong> <em> Modifier Les Informations &#58; {{ commande.codeC }} </em></strong></h2>
                    </div>
    <div class="box-body">
    {{ form_start(edit_form) }}
         <div class="form-group">
                                <h4><i class="fa fa-pencil-square"> Code </i> </h4>
                                 {{ form_widget(edit_form.codeC) }}
            </div>
            <div class="form-group">
                                <h4><i class="fa fa-pencil-square"> Date </i> </h4>
                                {{ form_widget(edit_form.dateC) }}
            </div>
            <div class="form-group">
                                <h4><i class="fa fa-pencil-square"> Montant </i> </h4>
                                {{ form_widget(edit_form.montant) }}
            </div>
            <div class="form-group">
                                <h4><i class="fa fa-pencil-square"> Adresse </i> </h4>
                                {{ form_widget(edit_form.adresseC) }}
            </div>
            <div class="form-group">
                                <h4><i class="fa fa-pencil-square"> Décision </i> </h4>
                                {{ form_widget(edit_form.validation) }}
            </div>              
        <div class="box-footer">
                                <a href="{{ path('commande_index') }}" class="btn btn-default"> Annuler</a>
                                {{ form_start(delete_form) }}
                                <button type="submit" class="pull-right btn  btn-danger btn-sm"> <i class="fa fa-trash-o">
                                    </i> Supprimer
                                </button>
                                {{ form_end(delete_form) }}
                                {{ form_widget(edit_form.Save) }}
                            </div>
                            {{ form_end(edit_form) }}
                        </div>
                </div>
            </div>
        </div>
    </section>
  
{% endblock %}
